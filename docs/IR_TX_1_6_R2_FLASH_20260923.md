# IR TX 1.6 R2: Authorized Flash and Cold Stationary Check

2026-09-23 approximately 19:34 PDT. Operator asked to proceed, requested setup
instructions, then confirmed Toby off. IR car only; no motor/AUTO commands.

- Reviewed source: `3887f2a`; re-review disposition recorded at `0c236cd`.
- USB `/dev/cu.wchusbserial110`; no existing owner reported by lsof.
- esptool read-mac independently verified IR car `38:18:2b:30:8c:2c`, not Toby.
- Recompiled unchanged reviewed sources for ESP32 core 3.3.11: 901,412 bytes
  flash, 64,288 bytes static RAM.
- Flashed at **115200 baud**, DIO/80 MHz/4 MB. All written hashes verified.
- Application binary SHA256:
  `041ad13fcfeeded7153511cdec71fa8888ba7548dab042265015e89268896b05`.
- Reset only the sensor ESP32 for a bounded 22-second serial startup capture.
  READY and MOVE both identify TX 1.6 R2. Boot ID `8dc31ef4e9f8b7c4`.

Cold stationary MOVE reports: zero rises/completions, reason 1
(INADEQUATE_CONTRAST), reported percentile span 47, saturation zero. The
startup gap counter is 1; assess later count growth separately. Cold unavailable
is expected before optical acquisition, not a failure of learned-stop retention.

Startup banner's WiFi.macAddress() printed `00:00:00:00:00:00`, unlike the MAC
read by esptool. Recorded as a diagnostic anomaly, not silently corrected or
proof of successful reception. No FATAL output; local senderr remained zero.
Receiver delivery, epoch, dashboard and noise peak-to-peak not verified here.

Raw capture: `field-records/logs/20260923_ir_tx_1_6_r2_boot.log`.
This is flash/startup evidence only. No hand-roll, learned plateau hold,
illumination sweep, physical accuracy or NAV/AUTO acceptance yet.
Next: supervised hand-roll capture, then low/high stationary retention and
the remaining independent-review bench checks. Toby remains off.

The older NGR-Files Arduino TX folder was not used or overwritten. Flash came
from the canonical repo sketch; reconcile that old folder before any later
IDE reflash so it cannot silently restore older firmware.
