#pragma once
// ============================================================================
// credentials_template.h  —  fill-in guide (safe to commit).
// Copy this file to credentials.h and replace the placeholders with the
// real values.  credentials.h is git-ignored and must never be committed.
// ============================================================================

// Shared WiFi
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

// Per-loco Blynk auth tokens
#define BLYNK_AUTH_TOKEN_9950011 "OTTO_BLYNK_AUTH_TOKEN"
#define BLYNK_AUTH_TOKEN_9950012 "TOBY_BLYNK_AUTH_TOKEN"
